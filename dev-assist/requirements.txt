# dev-assist dependencies
# ─────────────────────────────────────────────────────────────────────────────

# ── AI Engine (choose one) ───────────────────────────────────────────────────
ollama                        # local AI — recommended

# openai                      # OpenAI API
# groq                        # Groq API (fast free tier)

# ── HTTP / packaging (required — ollama-main CLI + plugins + web API) ────────
requests>=2.28                # GitHub API, Ollama REST, plugin requests
packaging>=23.0               # version parsing for ollama-main update checks

# ── Config validation (strongly recommended) ─────────────────────────────────
pydantic>=2.0

# ── Better terminal UI (recommended) ─────────────────────────────────────────
rich>=13.0
prompt-toolkit>=3.0

# ── Prompt templates (optional) ──────────────────────────────────────────────
jinja2>=3.1

# ── Web UI (required for --web mode) ─────────────────────────────────────────
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9

# ── Image attachments (resize/compress before sending to vision models) ─────
Pillow>=10.0

# ── Dev / Linting ────────────────────────────────────────────────────────────
# ruff  black  isort  pyright  pre-commit  pytest  pytest-asyncio
